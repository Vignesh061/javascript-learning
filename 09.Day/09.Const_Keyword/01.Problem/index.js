//const = a variable that cant be changed

const PI=3.14159;
let radius
let circumferance;

radius = window.prompt("Enter the radius of a cirule:")
radius=Number(radius);

circumferance=2*PI*radius;
console.log(circumferance);